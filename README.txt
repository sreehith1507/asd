Open these files directly in your browser:

1) public/login.html  (Collector)
2) public/login-admin.html  (Admin)
3) public/dashboard.html (auto-redirects to login if not logged in)

Demo credentials:
- collector / collector123
- admin / admin123

Features:
- Camera capture (getUserMedia)
- Image upload
- Local EXIF parsing (resolution, GPS if available, capture date)
- No server required; all runs client-side 